{
    "name": "web-system-samples",
    "private": true,
    "scripts": {
        "format": "prettier . --write",
        "format:spring-boot": "prettier ./packages/spring-boot/ --write --ignore-path /dev/null"
    },
    "devDependencies": {
        "prettier": "3.6.2",
        "prettier-plugin-java": "^2.7.5",
        "prettier-plugin-organize-imports": "^4.3.0"
    }
}
